# {{ ansible_managed }}
# Personal Access Token (classic) or fine-grained PAT with permissions to
# manage self-hosted runners (see docs/github-runner.md for the exact scopes).
ACCESS_TOKEN={{ github_runner_access_token }}
